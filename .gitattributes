# Auto detect text files and perform LF normalization
* text=auto

# PowerShell 脚本必须保持 CRLF + UTF-8 BOM:
#   - BOM 是硬要求。PowerShell 5.1 (Windows 自带的那个) 会把无 BOM 的 .ps1 按系统
#     ANSI 代码页解读, 脚本里的中文全变乱码, 连引号配对都会崩, 直接无法运行。
#     Git 本身不会动 BOM, 但请不要用会"顺手去掉 BOM"的编辑器保存。
#   - eol=crlf 保证在任何平台 clone 出来都是 Windows 原生行尾。
*.ps1 text eol=crlf

# 唯一的例外: install.ps1 必须"无 BOM"且保持纯 ASCII。
# 它是给 `irm ... | iex` 用的 —— irm 把文件内容交给解析器时是一个字符串,
# BOM (U+FEFF) 会留在字符串开头, 而它不是合法 token: 开头的 <# 就不再被当成注释,
# 帮助文本会被当代码解析, 报出指向某个中文标点的 "Missing argument in parameter list"。
# 把 <# 挪到第二行也没用, BOM 会和后面的内容粘成一个 token 被当命令名。
# 既然不能带 BOM, 它就只能是纯 ASCII, 否则有人下载下来直接运行时中文会乱码。


# 文档统一 LF, 避免跨平台 diff 噪音
*.md text eol=lf
